CREATE TABLE IF NOT EXISTS `agent_affiliation_event` (
  `agent_affiliation_id` int(10) unsigned NOT NULL COMMENT "Foreign Key, Primary Key",
  `event_id` int(10) unsigned NOT NULL COMMENT "Foreign Key, Primary Key",
  PRIMARY KEY  (`agent_affiliation_id`,`event_id`)
) ENGINE=InnoDB COMMENT="Maps a specific event to a controlling agent for debt consolidation";
